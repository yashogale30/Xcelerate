https://leetcode.com/problems/sum-of-digits-of-string-after-convert/

class Solution {
public:
    
    ## pehle string ke saare alphabets ko string integer banake ek string of number banao
    ## then woh string ke har ek element ko stoi se int banake ek sum variable me add karo       ##then wapis repeat this process on sum for k-1 times(ek bar already ho gya)
    int getLucky(string s, int k) 
    {
        string num="";
        for(auto c: s)
        {
            num+=to_string(c-'a'+1);
        }
        string num1 = num;
        int sum = 0;
        for(int i = 0;i<k;i++)
        {
            for(auto c:num1)
            {
                sum+=(c-'0');
            }
            num1 = to_string(sum);
            sum = 0;
        }
        return stoi(num1);
    }
};
